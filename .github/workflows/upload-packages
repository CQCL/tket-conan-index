#!/usr/bin/env bash

set -eux

for recipe in "$@"
do
    echo "Installing ${recipe} ..."
    conan create recipes/${recipe} --user=tket --channel=stable
    echo "Uploading ${recipe} ..."
    conan upload ${recipe}@tket/stable
done

echo "Done."
